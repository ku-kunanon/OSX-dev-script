{
	"shell_cmd": "go build \"${file}\" -o \"${file_path}/${file_base_name}\"",
	"file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$",
	"working_dir": "${file_path}",
	"selector": "source.go",

	"variants":
	[
		{
			"name": "Run",
			"shell_cmd": "go build -o \"${file_path}/${file_base_name}\" \"${file}\" && \"/usr/local/bin/sublime-terminal\" \"${file_path}/${file_base_name}\""
		}
	]
}
